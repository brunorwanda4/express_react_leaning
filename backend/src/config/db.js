const mysql = require("mysqli2")
const app = mysqli.createConnection({
    host : "localhost",
    password : "",
    username : "root",
    database : "sos_mi_db"
})

db.connect(errv=> {
    console.log({err})
}

)

module.exports(db)
